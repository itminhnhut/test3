import React from 'react';
import Network from './Network';

const index = () => {
    
    return (
        <div className="h-8 w-full border-t border-divider-dark bg-bgPrimary-dark fixed z-10 bottom-0 px-6 flex items-center">
            <Network />
        </div>
    );
};

export default index;
